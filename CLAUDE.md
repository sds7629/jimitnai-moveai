# 작업 워크플로우 규칙

## Worktree 기반 작업 강제

이 레포의 모든 코드 작업은 `main` 브랜치에서 직접 진행하지 않는다. 반드시 아래 절차를 따른다.

1. 작업을 시작하기 전, 별도의 git worktree(및 전용 브랜치)를 생성한다.
   ```
   git worktree add ../<worktree-dir> -b <feature-branch>
   ```
2. 해당 worktree 안에서만 코드 변경, 커밋, 테스트를 진행한다. `main`이 체크아웃된 원본 작업 디렉토리는 건드리지 않는다.
3. 작업이 끝나고 `main`으로 병합(merge)하기 전, 아래 두 가지가 준비되어야 한다.
   - **추가된 기능 요약**: 이번 작업에서 어떤 기능/변경 사항이 추가되었는지
   - **테스트 결과**: 관련 테스트 케이스를 실행한 결과 (통과/실패 개수, 실패 시 원인 등)
4. 새로 추가되거나 변경된 기능 각각에 대해 최소 3개 이상의 테스트 케이스를 작성한다. 정상 동작(happy path) 1개만으로는 부족하며, 경계값(edge case)이나 예외/오류 상황을 포함해 최소 3개를 채운다.
5. 커밋 메시지는 반드시 위 항목들(기능 설명 + 테스트 결과)을 바탕으로 작성한다. 테스트를 실행하지 않았거나 테스트가 없는 변경이라면 그 사실도 명시한다.
6. 병합 후에는 사용이 끝난 worktree를 정리한다.
   ```
   git worktree remove <worktree-dir>
   ```

## 커밋 메시지 형식 예시

```
<기능 요약 한 줄>

- 추가/변경된 기능: ...
- 테스트 결과: X개 중 Y개 통과 (실패: ...) / 또는 "테스트 없음"
```

## 병합 전 체크리스트

- [ ] 별도 worktree/브랜치에서 작업했는가
- [ ] 기능 변경 내용을 파악했는가
- [ ] 변경/추가된 기능마다 테스트 케이스가 3개 이상인가
- [ ] 테스트를 실행하고 결과를 확인했는가
- [ ] 커밋 메시지에 기능 설명과 테스트 결과가 포함되어 있는가
- [ ] `main`으로 병합 후 worktree를 정리했는가
